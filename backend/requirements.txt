# AffiGuard – Python dependencies
# Render free tier: 512 MB RAM — Playwright disabled by default

Flask==3.0.3
flask-cors==4.0.1
Werkzeug==3.0.3
gunicorn==22.0.0

supabase==2.5.0
python-dotenv==1.0.1
bcrypt==4.1.3
requests==2.32.3

# Playwright is optional (Layer 3 detection).
# On Render free tier (512 MB), DO NOT install playwright — it causes OOM.
# Set ENABLE_PLAYWRIGHT=false in Render env vars (default is already false).
# If you upgrade to a paid Render instance (2 GB+), you can uncomment:
# playwright==1.44.0
