// config.js — set your Koyeb backend URL here before deploying to Vercel
// Replace the URL below with your actual Koyeb app URL.
// Local dev: leave as '' so fetch() calls go to same-origin (Flask serves everything).
window.__BACKEND_URL = '';
